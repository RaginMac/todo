using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CheckCoinDropLimit : MonoBehaviour {

	public int noOfUnitsCoins, noOfTensCoins, noOfOnesCoins;
	public DistributedCoins coins;

	public Transform[] snapArray, snapArray2;
	public int dropindex = 0, tenCounter = 0, dropIndex2 = 0, oneCounter = 0;

	// Use this for initialization
	void Start () {
	//	SetDropLimit(coins.tensDigit, coins.numberTwo);
	}
	
	// Update is called once per frame
	void Update () {
		
	}

	public void SetDropLimit(int n1, int n2)
	{
		noOfTensCoins = n1 / n2;
	}
}
